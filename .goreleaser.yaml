version: 2

project_name: kubestellar-mcp

before:
  hooks:
    - go mod tidy

builds:
  - id: kubestellar-ops
    main: ./cmd/kubestellar-ops
    binary: kubestellar-ops
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/kubestellar/klaude/internal/version.Version={{.Version}}
      - -X github.com/kubestellar/klaude/internal/version.BuildDate={{.Date}}
      - -X github.com/kubestellar/klaude/internal/version.GitCommit={{.Commit}}

  - id: kubestellar-deploy
    main: ./cmd/kubestellar-deploy
    binary: kubestellar-deploy
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/kubestellar/klaude/internal/version.Version={{.Version}}
      - -X github.com/kubestellar/klaude/internal/version.BuildDate={{.Date}}
      - -X github.com/kubestellar/klaude/internal/version.GitCommit={{.Commit}}

archives:
  - id: kubestellar-ops
    builds:
      - kubestellar-ops
    format: tar.gz
    name_template: "kubestellar-ops_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

  - id: kubestellar-deploy
    builds:
      - kubestellar-deploy
    format: tar.gz
    name_template: "kubestellar-deploy_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: kubestellar
    name: klaude
  draft: false
  prerelease: auto

brews:
  - name: kubestellar-ops
    ids:
      - kubestellar-ops
    repository:
      owner: kubestellar
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/kubestellar/klaude"
    description: "Multi-cluster Kubernetes diagnostics, RBAC analysis, and security checks"
    license: "Apache-2.0"
    install: |
      bin.install "kubestellar-ops"
    test: |
      system "#{bin}/kubestellar-ops", "version"

  - name: kubestellar-deploy
    ids:
      - kubestellar-deploy
    repository:
      owner: kubestellar
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/kubestellar/klaude"
    description: "App-centric multi-cluster deployment and operations for Kubernetes"
    license: "Apache-2.0"
    install: |
      bin.install "kubestellar-deploy"
    test: |
      system "#{bin}/kubestellar-deploy", "version"
